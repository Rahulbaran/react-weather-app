export default function Weather({ weatherData }) {
  console.table(weatherData);

  return (
    <main className="container weather-container">
      <h1>WELCOME TO WEATHER CONTAINER</h1>
    </main>
  );
}
